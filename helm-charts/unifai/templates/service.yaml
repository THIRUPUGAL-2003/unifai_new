apiVersion: v1
kind: Service
metadata:
  name: {{ include "unifai.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "unifai.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.unifai.cluster.enabled }}
    - port: {{ .Values.unifai.cluster.gossip.port }}
      targetPort: gossip
      protocol: TCP
      name: gossip
    - port: {{ .Values.unifai.cluster.gossip.port }}
      targetPort: gossip-udp
      protocol: UDP
      name: gossip-udp
    {{- if .Values.unifai.cluster.grpc }}
    - port: {{ .Values.unifai.cluster.grpc.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    {{- end }}
    {{- end }}
  selector:
    {{- include "unifai.serverSelectorLabels" . | nindent 4 }}
