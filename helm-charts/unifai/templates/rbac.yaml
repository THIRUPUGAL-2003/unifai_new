{{- /*
Create pod-discovery RBAC only when all of these are true:
1) rbac.podDiscovery.enabled
2) cluster mode is enabled
3) cluster discovery is enabled
4) discovery type is kubernetes
*/ -}}
{{- if and .Values.rbac.podDiscovery.enabled .Values.unifai.cluster.enabled .Values.unifai.cluster.discovery.enabled (eq .Values.unifai.cluster.discovery.type "kubernetes") -}}
{{- $podDiscoveryRoleName := printf "%s-pod-discovery" (include "unifai.fullname" . | trunc 49 | trimSuffix "-") -}}
{{- $discoveryNamespace := .Values.unifai.cluster.discovery.k8sNamespace | default .Release.Namespace -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ $podDiscoveryRoleName }}
  namespace: {{ $discoveryNamespace }}
  labels:
    {{- include "unifai.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ $podDiscoveryRoleName }}
  namespace: {{ $discoveryNamespace }}
  labels:
    {{- include "unifai.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "unifai.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ $podDiscoveryRoleName }}
{{- end -}}
