# UnifAI Implementation docs — INDEX ONLY

Full guides are **separate**. Open these:

1. **Server** (install / upgrade / uninstall) → `SERVER_IMPLEMENTATION.md` (+ `.pdf` / `.docx`)
2. **Windows Guard** (install / upgrade / uninstall) → `WINDOWS_GUARD_IMPLEMENTATION.md` (+ `.pdf` / `.docx`)
3. **Configuration** (all settings) → `CONFIGURATION.md` (+ `.pdf` / `.docx`)

Master index: `docs/README.md`
